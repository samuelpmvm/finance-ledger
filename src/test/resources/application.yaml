spring:
  profile:
    active: test
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:finance-ledger_db}
    username: ${DB_USER:finance-ledger}
    password: ${DB_PASSWORD:finance-ledger}
    jpa:
      hibernate:
        ddl-auto: update
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yaml
  security:
    oauth2:
      resourceserver:
        jwt:
          enabled: false